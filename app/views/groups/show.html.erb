<h2 class="title is-3"><%= "All #{@group.name} Activities" %></h2>
<% @group_activities.each do |activity| %>
  <p><%= activity.name %> </p>
  <p><%= "#{activity.amount} minutes" %> </p>
  <p><%= "#{activity.created_at}" %> </p>
  <hr>
<% end %>