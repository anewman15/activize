
<% @groups.each do |group| %>
  <p><%= link_to group.name, user_group_path(current_user, group) %> </p>
  <% if group.icon.attached? %>
    <img src="<%= url_for(group.icon) %>">
  <% end %>
  <hr>
<% end %>
<%= create_group_button(current_user) %>